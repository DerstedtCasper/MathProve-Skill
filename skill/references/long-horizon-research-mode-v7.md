# Long-Horizon Research Mode

Use this when a proof may run for hours or days. The orchestrator must not skip decomposition, refutation, or final audit to save tokens. It should partition the lemma DAG into clusters, checkpoint after each gate, write failure/success queues, and periodically refactor to avoid proof debt.
